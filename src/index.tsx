import React from 'react'
import ReactDOM from 'react-dom'
import CalculatorContainer from './Calculator/CalculatorContainer'

const root = document.getElementById('root')

ReactDOM.render(<CalculatorContainer />, root)
